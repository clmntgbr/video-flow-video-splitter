# Development environment override
services:
  sound-extractor:
      build:
          dockerfile: ./Dockerfile
          target: dev
          args:
              SERVICE_PORT: 5000
              SERVICE_NAME: sound-extractor
